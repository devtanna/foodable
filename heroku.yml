build:
  docker:
    front: docker/front/Dockerfile
    back: docker/back/Dockerfile
    nginx: docker/nginx/Dockerfile
  config:
    NODE_ENV: production
run:
  front:
    command:
      - npm run prod