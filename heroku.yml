build:
  docker:
    front: Dockerfile.front
    back: Dockerfile.back
    nginx: Dockerfile.nginx
  config:
    NODE_ENV: production
    ENABLE_HEROKU: true
run:
  front:
    command:
      - npm run prod